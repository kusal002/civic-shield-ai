"use client";

import { Eye, ShieldCheck, Trash2 } from "lucide-react";
import { useState } from "react";

import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import type { ReportStatus, UrgencyLevel } from "@/types/report";

type ModeratorReport = { id: string; description: string; locationLabel: string; duration: string; affectedPeople: number | null; extraDetails: string | null; attachmentCount: number; category: string | null; urgency: UrgencyLevel | null; routeName: string | null; status: ReportStatus; createdAt: string; updatedAt: string; emailRecipient: string | null; gmailMessageId: string | null };
const statuses: ReportStatus[] = ["acknowledged", "assigned", "in-progress", "department-resolved", "verification-pending", "verified-resolved", "disputed", "reopened", "overdue"];

export function ModeratorWorkspace() {
  const [accessKey, setAccessKey] = useState(""); const [signedIn, setSignedIn] = useState(false); const [reports, setReports] = useState<ModeratorReport[]>([]); const [selected, setSelected] = useState<ModeratorReport | null>(null); const [status, setStatus] = useState<ReportStatus>("acknowledged"); const [note, setNote] = useState(""); const [message, setMessage] = useState(""); const [loading, setLoading] = useState(false); const [deleteOpen, setDeleteOpen] = useState(false); const [deleting, setDeleting] = useState(false);
  async function loadReports() { setLoading(true); try { const response = await fetch("/api/moderator/reports", { cache: "no-store" }); const payload = await response.json() as { reports?: ModeratorReport[]; error?: string }; if (!response.ok) throw new Error(payload.error ?? "Could not load reports."); setReports(payload.reports ?? []); setSelected((current) => current ? (payload.reports ?? []).find((report) => report.id === current.id) ?? null : null); } catch (error) { setMessage(error instanceof Error ? error.message : "Could not load reports."); } finally { setLoading(false); } }
  async function login() { setMessage(""); const response = await fetch("/api/moderator/login", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ accessKey }) }); const payload = await response.json() as { error?: string }; if (!response.ok) return setMessage(payload.error ?? "Could not sign in."); setSignedIn(true); setAccessKey(""); await loadReports(); }
  async function update() { if (!selected) return; const response = await fetch(`/api/moderator/reports/${encodeURIComponent(selected.id)}/status`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ status, note }) }); const payload = await response.json() as { error?: string }; if (!response.ok) return setMessage(payload.error ?? "Update failed."); setMessage("Status update published to the public timeline."); setNote(""); await loadReports(); }
  async function removeReport() { if (!selected) return; setDeleting(true); const response = await fetch(`/api/moderator/reports/${encodeURIComponent(selected.id)}`, { method: "DELETE" }); const payload = await response.json() as { error?: string }; setDeleting(false); if (!response.ok) return setMessage(payload.error ?? "Delete failed."); setDeleteOpen(false); setSelected(null); setMessage("Report deleted."); await loadReports(); }
  return <><main className="min-h-screen bg-canvas p-6 text-ink sm:p-10"><div className="mx-auto max-w-7xl"><p className="eyebrow">Protected workspace</p><h1 className="mt-3 flex items-center gap-2 font-display text-3xl font-bold"><ShieldCheck className="text-brand" /> Moderator controls</h1>{!signedIn ? <Card className="mt-6 max-w-xl rounded-3xl"><CardContent className="p-6"><p className="text-sm leading-6 text-muted">Sign in with the server-configured moderator access key.</p><input className="mt-5 h-12 w-full rounded-xl border border-line px-3" type="password" value={accessKey} onChange={(event) => setAccessKey(event.target.value)} placeholder="Moderator access key" /><Button className="mt-3" onClick={() => void login()}>Sign in</Button>{message ? <p className="mt-4 text-sm font-semibold text-danger">{message}</p> : null}</CardContent></Card> : <div className="mt-6 grid gap-6 lg:grid-cols-[0.85fr_1.15fr]"><Card className="rounded-3xl"><CardContent className="p-5"><div className="flex items-center justify-between"><div><p className="font-display text-xl font-bold">Reports by date</p><p className="mt-1 text-sm text-muted">Newest first</p></div><Button size="sm" variant="outline" onClick={() => void loadReports()}>{loading ? "Loading…" : "Refresh"}</Button></div><div className="mt-5 space-y-2">{reports.length ? reports.map((report) => <button className={`w-full rounded-2xl border p-3 text-left transition ${selected?.id === report.id ? "border-brand bg-brand-soft" : "border-line bg-[#fbfdfc] hover:border-brand/40"}`} key={report.id} onClick={() => { setSelected(report); setStatus(report.status === "ready-to-analyze" || report.status === "delivery-confirmed" ? "acknowledged" : report.status); setNote(""); }}><div className="flex items-center justify-between gap-2"><span className="font-mono text-xs font-bold text-brand">{report.id}</span><Badge tone={report.status === "verified-resolved" ? "safe" : "caution"}>{report.status.replaceAll("-", " ")}</Badge></div><p className="mt-2 text-sm font-bold">{report.category ?? "Unclassified issue"}</p><p className="mt-1 text-xs text-muted">{formatDate(report.createdAt)}</p></button>) : <p className="rounded-xl bg-[#fbfdfc] p-4 text-sm text-muted">No reports found.</p>}</div></CardContent></Card><Card className="rounded-3xl"><CardContent className="p-6 sm:p-7">{selected ? <><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="font-mono text-xs font-bold text-brand">{selected.id}</p><h2 className="mt-2 font-display text-2xl font-bold">{selected.category ?? "Civic issue"}</h2></div><Button variant="danger" size="sm" onClick={() => setDeleteOpen(true)}><Trash2 size={15} /> Delete false report</Button></div><div className="mt-6 grid gap-4 sm:grid-cols-2"><Info label="Submitted" value={formatDate(selected.createdAt)} /><Info label="Location" value={selected.locationLabel} /><Info label="Issue description" value={selected.description} /><Info label="Extra details" value={selected.extraDetails || "None"} /><Info label="Evidence" value={`${selected.attachmentCount} file(s), stored privately`} /><Info label="Recipient" value={selected.emailRecipient || "Not sent"} /></div><div className="mt-6 border-t border-line pt-6"><p className="eyebrow">Publish verified action</p><select className="mt-3 h-12 w-full rounded-xl border border-line px-3" value={status} onChange={(event) => setStatus(event.target.value as ReportStatus)}>{statuses.map((item) => <option key={item} value={item}>{item.replaceAll("-", " ")}</option>)}</select><textarea className="mt-3 min-h-28 w-full rounded-xl border border-line p-3" value={note} onChange={(event) => setNote(event.target.value)} maxLength={280} placeholder="Public status note (required, 280 characters maximum)" /><Button className="mt-3" disabled={!note.trim()} onClick={() => void update()}><Eye size={16} /> Publish status update</Button></div></> : <div className="grid min-h-72 place-items-center text-center"><div><Eye className="mx-auto text-brand" size={28} /><p className="mt-4 font-display text-xl font-bold">Select a report</p><p className="mt-2 text-sm text-muted">Read the full private report before publishing an action.</p></div></div>}{message ? <p className="mt-5 text-sm font-semibold text-brand">{message}</p> : null}</CardContent></Card></div>}</div></main>{deleteOpen && selected ? <DeleteDialog reportId={selected.id} deleting={deleting} onCancel={() => setDeleteOpen(false)} onDelete={() => void removeReport()} /> : null}</>;
}
function DeleteDialog({ reportId, deleting, onCancel, onDelete }: { reportId: string; deleting: boolean; onCancel: () => void; onDelete: () => void }) { return <div className="fixed inset-0 z-50 grid place-items-center bg-[#132421]/55 p-5" role="dialog" aria-modal="true" aria-labelledby="delete-title"><Card className="w-full max-w-lg rounded-3xl shadow-surface"><CardContent className="p-6 sm:p-7"><span className="grid size-11 place-items-center rounded-2xl bg-[#fff1ef] text-danger"><Trash2 size={21} /></span><p className="eyebrow mt-5 text-danger">Permanent action</p><h2 className="mt-2 font-display text-2xl font-bold" id="delete-title">Delete this report?</h2><p className="mt-3 text-sm leading-6 text-muted">Deleting <strong className="text-ink">{reportId}</strong> permanently removes the report, its public timeline, and community-verification signals. This cannot be undone.</p><div className="mt-6 flex flex-col-reverse gap-2 sm:flex-row sm:justify-end"><Button variant="outline" disabled={deleting} onClick={onCancel}>Keep report</Button><Button variant="danger" disabled={deleting} onClick={onDelete}><Trash2 size={16} /> {deleting ? "Deleting…" : "Delete permanently"}</Button></div></CardContent></Card></div>; }
function Info({ label, value }: { label: string; value: string }) { return <div><p className="text-xs font-bold uppercase tracking-[0.1em] text-muted">{label}</p><p className="mt-1 whitespace-pre-wrap text-sm leading-6">{value}</p></div>; }
function formatDate(value: string) { return new Date(value).toLocaleString("en-IN", { day: "numeric", month: "short", year: "numeric", hour: "numeric", minute: "2-digit" }); }
